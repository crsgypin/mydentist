<%= render partial: "admin/common/index_table" , locals: {
  id: "members",
  resources: @members,
  cols: [
    {k: "id", v: proc{|a| a.id }},
    {t: "level", v: proc{|a| a.level }},
    {t: "email", v: proc{|a| a.email }},
    {t: "username", v: proc{|a| a.username }},
    {t: " ", v: proc{|a| 
      link_to "刪除", url_for(action: :destroy, id: a), remote: true, method: :delete, data: {confirm: "確定移除?"}
    }},
  ],
  row: {
    default_sel_index: 0,
    iframe_link_url: proc {|a| url_for(action: :show, id: a)}
  },
  new: {
    link_url: url_for(action: :new),
    btn_name: "新增會員"
  }
}
%>
