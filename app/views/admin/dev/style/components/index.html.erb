<%= render partial: "admin/common/index_table" , locals: {
  id: "components",
  resources: @components,
  cols: [
    {t: "key", v: proc{|a| a[:key] }},
    {t: "name", v: proc{|a| a[:name] }},
  ],
  row: {
    default_sel_index: 0,
    iframe_link_url: proc {|a| url_for(action: :show, id: a[:key])}
  }
}
%>
