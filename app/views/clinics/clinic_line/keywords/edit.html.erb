
<%= form_tag url_for(action: :update), method: "patch" do %>
<%= render partial: "clinics/clinic_line/common/line-template-form", locals: {
	title: "編輯關鍵字",
	questions: @clinic_line_keyword.line_template.keywords.map do |keyword|
		r = {
			name: keyword.name
		}
	end,
	replies: @clinic_line_keyword.line_template.template_messages.map do |message|
		r = {
			content: message.content
		}
	end,
	cancel_url: url_for(action: :index)
} %>
<% end %>
