
<%= form_tag url_for(action: :update), method: "patch", remote: true do %>
<%= render partial: "clinics/clinic_line/common/line-template-form", locals: {
	title: "編輯關鍵字",
	keywords: @clinic_line_keyword.line_template.keywords.map do |keyword|
		r = {
			id: keyword.id,
			name: keyword.name
		}
	end,
	template_messages: @clinic_line_keyword.line_template.template_messages.map do |message|
		r = {
			id: message.id,
			content: message.content
		}
	end,
	cancel_url: url_for(action: :index)
} %>
<% end %>
