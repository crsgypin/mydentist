
<%= render partial: "clinics/clinic_line/common/line-templates-box", locals: {
	line_templates: @clinic_line_knowledges.map do |knowledge|
		r = {
			title: knowledge.line_template.title,
			content: knowledge.line_template.content,
			edit_path: url_for(action: :edit, id: knowledge)
		}
	end,
} %>
