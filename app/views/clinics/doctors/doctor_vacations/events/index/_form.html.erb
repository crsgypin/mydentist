
<%= render partial: 'clinics/common/vacations_form/event_notifications_table', locals: {
	notifications: @events.map do |event|
		r = {
			event: event,
			args: {
				clinic_name: @clinic.name,
				doctor_name: @doctor.name				
			}.to_json
		}
	end,
	query_path: url_for,
	notification_schedule_path: new_clinic_event_notification_schedule_path(@clinic, category: "診所休假修改掛號"),
} %>
