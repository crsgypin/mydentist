
<%= render partial: 'clinics/common/vacations_form/event_notifications_table', locals: {
	event_objs: @events.map do |event|
		r = {
			event: event,
			args: {
				clinic_name: @clinic.name,
				doctor_name: @doctor.name				
			}
		}
	end,
	query_path: url_for,
	notification_template_path: clinic_event_notification_template_path(@clinic, @event_notification_template),
} %>
