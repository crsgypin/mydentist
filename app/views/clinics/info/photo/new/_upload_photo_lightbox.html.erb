
<%= render partial: "clinics/common/upload_photo_lightbox", locals: {
	form_url: url_for(action: :create),
	form_method: "post",
	photo_url: @clinic.photo_url,
	photo_input_name: "clinic[photo]"
} %>

